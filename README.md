# eks-vpc-terraform

Terraform example to deploy EKS in private VPC with autos-calling group.

### Install

```bash
$ terraform init
$ terraform apply
```

### Destroy

```bash
$ terraform init
$ terraform destroy
```
